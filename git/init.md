```sh
# 克隆项目 [有初始readme文件]
git clone git@github.com:ryanxchen/react-project-template.git
# 设置 origin
git remote add origin git@github.com:ryanxchen/react-project-template.git
# 设置上游
# git push --set-upstream origin master
# 允许合并不相干历史
git pull origin master --allow-unrelated-histories
# marge
# push
git push
```